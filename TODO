- Check for line references in autoref
- Update algorithms with correct formatting of functions, variables etc., \gets verwenden usw.
- Algorithmen in Orchestration auslagern
- Formatierung Algorithmen: Linien statt end-Zeilen
- Transformationset should be lower case, like all other set/tuple variables, not upper case - seems to work fine, so we should really do it

- Unify algorithm labels
- Unify theorem/lemma etc. labels
- Give names to theorems/lemmas etc.


- Elemente aus Sequenzen statt mit t_1 mit t[1] adressieren?
- Anwendung eines Changetuple einführen und überall wo einzelne Changes angewendet werden ersetzen.
** Makros sind eingeführt
** Bereits in Orchestrierung durchgezogen

- Sequenzvariablen als T[] darstellen, Sequenzen als [T1, T2, ..] und Notation einführen. Wenn Sequenzen und Tupel unterschieden werden sollen, z.B. auch für Orchestrierungen
-- Definieren, dass T[]] + t = [T1, ..., Tn, t], falls T[] = [T1, ..., Tn]
-- Zugriff auf Element i einer Sequent mit T[i]
-- Klar machen, dass mathematisch eigentlich äquivalent zu Tupeln, aber so Unterscheidung der Semantik anhand der Syntax einfacher
** Bereits durchgezogen in Orchestrierung


- Figures aufräumen -> in richtige Ordner