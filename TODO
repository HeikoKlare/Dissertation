- Check for line references in autoref
- Update algorithms with correct formatting of functions, variables etc., \gets verwenden usw.
- Algorithmen in Orchestration auslagern
- Formierung Algorithmen: Linien statt end-Zeilen
- Transformationset should be lower case, like all other set/tuple variables, not upper case

- Unify algorithm labels
- Unify theorem/lemma etc. labels
- Give names to theorems/lemmas etc.

- Elemente aus Sequenzen statt mit t_1 mit t[1] adressieren?