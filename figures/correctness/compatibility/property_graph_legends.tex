\begin{tikzpicture}[
    every node/.style={font=\small, inner sep=0.5em},
    predicate/.style={thin, minimum height=4em, minimum width=14.8em+0.3\difftoafiveimage, text width=13.8em+0.3\difftoafiveimage, align=left, anchor=west}]
    
    % METAGRAPH SYSTEMS
    \node[predicate, draw=darkorange, name=n1] at (0, 0) {%
        $\mathvariable{Person.firstname} = \mathvariable{fstn}$ \\
        $\mathvariable{Person.lastname} = \mathvariable{lstn}$ \\
        $\mathvariable{Resident.name} = \mathvariable{fstn} + "\text{\textvisiblespace}" + \mathvariable{lstn}$
        %\lstinline[language=QVT]{Resident.name = fstn + ' ' + lstn}
    };

    \node[predicate, draw=darkgreen, name=n2] at (0, -4.5em) {%
        $\mathvariable{Person.firstname} = \mathvariable{fstn}$ \\
        $\mathvariable{Person.lastname} = \mathvariable{lstn}$ \\
        $\mathvariable{Employee.name} = \mathvariable{fstn} + "\text{\textvisiblespace}" + \mathvariable{lstn}$
        %\lstinline[language=QVT]{Employee.name = fstn + ' ' + lstn}
    };

    \node[predicate, draw=darkred, name=n3] at (0, -9em) {%
        $\mathvariable{Resident.name} = n$ \\
        $\mathvariable{Employee.name} = n$
    };
    
    \node[predicate, draw=darkblue, name=n4, anchor=west] at ({15.3em+0.4\difftoafiveimage}, 0) {%
        $\mathvariable{Resident.address} = \mathvariable{addr}$ \\
        $\mathvariable{Employee.address} = \mathvariable{addr}$
    };
    
    \node[predicate, draw=darkblue, name=n5, anchor=west] at ({15.3em+0.4\difftoafiveimage}, -4.5em) {%
        $\mathvariable{Person.income} = \mathvariable{inc}$ \\
        $\mathvariable{Employee.salary} = \mathvariable{inc}$
    };
    
    \node[predicate, draw=darkblue, name=n6, anchor=west] at ({15.3em+0.4\difftoafiveimage}, -9em) {%
        $\mathvariable{Person.socsecnumber} = \mathvariable{ssn}$ \\
        $\mathvariable{Employee.socsecnumber} = \mathvariable{ssn}$
    };

    \node[below left= 0 of n1.north east, anchor=north east] {\textbf{1}};
    \node[below left= 0 of n2.north east, anchor=north east] {\textbf{2}};
    \node[below left= 0 of n3.north east, anchor=north east] {\textbf{3}};
    \node[below left= 0 of n4.north east, anchor=north east] {\textbf{4}};
    \node[below left= 0 of n5.north east, anchor=north east] {\textbf{5}};
    \node[below left= 0 of n6.north east, anchor=north east] {\textbf{6}};
\end{tikzpicture}